vtest "Test tcp vmod"

feature cmd "uname | grep Linux"

server s1 {
       rxreq
       txresp
} -start

vinyl v1 -vcl+backend {
	import tcp;

	sub vcl_deliver {
		tcp.dump_info();
	}
} -start

client c1 {
	txreq -url "/"
	rxresp
	expect resp.status == 200
}

client c1 -run
